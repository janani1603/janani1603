def prRed(skk):
    print("\033[91m {}\033[00m" .format(skk))
user=input("Enter the word: ")
if user.isalpha():
    if len(user)%2==0:
        print([user])
    else:
        prRed("LIST IS EMPTY")
else:
    prRed("WRONG INPUT")
